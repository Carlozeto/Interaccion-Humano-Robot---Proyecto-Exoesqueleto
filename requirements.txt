# Core dependencies
depthai>=2.30.0
mediapipe>=0.10.0
opencv-python>=4.8.0
PyQt5>=5.15.0
numpy>=1.21.0
scipy>=1.7.0
flask>=2.0.0
blobconverter>=1.4.0

# Optional dependencies (used in some files)
pygame>=2.0.0
sounddevice>=0.4.0
requests>=2.25.0
Pillow>=8.0.0
matplotlib>=3.5.0

# Standard library imports (no need to install)
# threading, time, csv, json, datetime, os, math, sys
